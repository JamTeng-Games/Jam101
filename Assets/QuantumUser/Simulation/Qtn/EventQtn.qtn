event OnMove
{
    EntityRef entity;
    FPVector2 velocity;
}

event OnRotate
{
    EntityRef entity;
    FP Rotation;    // 弧度
}

event OnStopMove
{
    EntityRef entity;
}

event PlayAnim
{
    EntityRef entity;
    int animKey;
    bool force;
}

event OnChangeHp
{
    EntityRef entity;
    int modHp;
}

event OnHit
{
    EntityRef entity;
}

event OnDie
{
    EntityRef entity;
}